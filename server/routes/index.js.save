var express = require('express');
var router = express.Router();const axios = require('axios');
/* GET home page. */
router.get('/', function(req, res, next) {
  axios.get('http://checkip.amazonaws.com').then((data) => {
	return res.send(data)		
  })
  res.send('index');
});

module.exports = router;
