version: "3"
services:
  frontend:
    build: ./client
    restart: on-failure
    ports:
      - "3000:3000"

  backend:
    build: ./server
    restart: on-failure
    tty: true
    ports:
      - "5000:5000"
  redis:
    image: "redis:latest"
